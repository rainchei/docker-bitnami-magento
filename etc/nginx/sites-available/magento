upstream fastcgi_backend {
  server unix:/run/php/php7.2-fpm.sock;
}

server {
  listen 80;
  listen [::]:80;
  
  server_name localhost-magento.example.com;

  set $MAGE_ROOT /var/www/magento2;
  set $MAGE_MODE production;
  include /var/www/magento2/nginx.conf.sample;
}
